type HeroTypes = {
  image: Image;
  title?: string;
  desc?: string;
};

type Image = {
  width: number;
  height: number;
  src: string;
  alt: string;
};

type SectionParagraph = {
  title: string;
  content: string;
};
